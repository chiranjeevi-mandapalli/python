# numerator =int(input("Enter numerator")) # what ever the input takes is always a string so we should convert it
# denominator =int(input("Enter denominator"))
#
# res = numerator/denominator
# print(res)

# if we want the cursor to go to next line

numerator =int(input("Enter numerator\n")) # what ever the input takes is always a string so we should convert it
denominator =int(input("Enter denominator\n"))

res = numerator/denominator
print(res)
